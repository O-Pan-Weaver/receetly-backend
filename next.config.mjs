/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you had other options (images, redirects, etc.), put them here.
  // Example:
  // reactStrictMode: true,
};

export default nextConfig;
